"use strict"
let nums = [5, 8, 3, 6, 8];
let primerIndice = nums[0]; 
let ultimoIndice = nums[4];

alert(`El valor del primer índice es: ${primerIndice} y el valor del último índice es: ${ultimoIndice}`);